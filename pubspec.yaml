name: flutter_app_auth_template
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  equatable: ^1.2.5
  firebase_auth: ^0.20.0
  firebase_core: ^0.7.0
  flutter_bloc: ^6.1.1
  google_sign_in: ^4.5.9
  sqflite: any
  path_provider: ^1.6.10
  shared_preferences: ^0.5.12+4
  flutter_custom_clippers: ^1.1.2


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
